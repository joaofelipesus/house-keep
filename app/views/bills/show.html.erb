<p style="color: green"><%= notice %></p>

<%= render @bill %>

<div>
  <%= link_to t('.edit_bill'), edit_bill_path(@bill) %> |
  <%= link_to t('.back'), bills_path %>

  <%= button_to t('.destroy_bill'), @bill, method: :delete %>
</div>
